# 计划：换子职业元素 + 神器模组（修复两个做不到的能力）

状态：**待评审**（未开工）。目标版本：0.3.0（含破坏性/新增能力）。
本文只记事实、决定与验证方式；实现细节落到代码与注释里。

## 一、两个症状

1. **术士从棱镜切火，切不了**（"把子职业换成烈日"这类请求失败）。
2. **神器模组换不了**（`subclass_assistant(intent="equip_artifact_mod")` 装不上，除了极少数情况）。

## 二、已确认的事实（有出处）

### 2.1 游戏规则：元素属于"子职业物品"，不是插槽

- DIM `src/app/inventory/subclass.ts`：元素从**装备的那件子职业物品**读（`item.element.enumValue`，
  棱镜=Kinetic、火=Thermal、虚空=Void、冰=Stasis、缚丝=Strand），图标也按元素取。
- DIM `src/app/inventory/store/override-sockets.ts`：插槽写入模型是 `SocketOverrides = {socketIndex: plugHash}`，
  即**每个插件带着自己的槽索引**去插，没有"默认槽"这回事。

### 2.2 我们的代码为什么做不到

| 症状 | 根因（位置） |
| --- | --- |
| 换元素 | `services/subclass_service.py:258 modify_subclass` 只会把 plug 插进**当前子职业**的槽。拿"火的超能/近战"往棱镜的槽里插，上游必然拒 |
| 换元素（另一条路） | `services/loadout_subclass_sockets.py:150-160`：一旦"当前子职业 ≠ 保存/确认的子职业"就**直接失败**（`action="subclass"`, `detail="当前子职业与保存/确认的子职业不一致。"`）——这条路上也没有"换过去"的动作 |
| 神器模组 | `services/artifact_service.py` 装模组时 **`socket_index=0` 写死**。神器是按模组分槽的一排插槽，只有"恰好落在 0 号槽"的模组能成功 |

### 2.3 DIM 的正确做法（神器）

DIM `src/app/loadout/ArtifactPlugDrawer.tsx`：

```js
for (const socket of artifact.sockets.allSockets) {
  if (!socket.plugSet && socket.socketDefinition.socketTypeHash !== RESET_SOCKET) continue;
  const matchingPlug = remainingPlugs.find((plug) =>
    socket.plugSet?.plugs.some((option) => option.plugDef.hash === plug.hash));
  if (matchingPlug) newOverrides[socket.socketIndex] = matchingPlug.hash;  // 槽索引是算出来的
}
```

即：**遍历神器的槽，找"候选插件里包含目标模组"的那个槽**，用它自己的 `socketIndex` 去插。

## 三、目标与非目标

**目标**：把这两件事做成"能真做、做完能核对、失败说得清"的能力。

**非目标**：
- 不做"一键换整套子职业 DIM 式预设"（那是 loadout 的活，已存在）；
- 不碰武器/护甲的插槽写入路径（它们已经工作正常）；
- 不为旧行为留兼容分支。

## 四、分阶段

### P0 只读勘探（不改账号）

- 用项目自己的 `services/profile_components.py` 常量取档（**不要手写组件号**：第一次勘探我用了猜的
  205/305/310，被上游拒了），读出：
  - 术士当前神器实例的 socket 列表（组件 305）与每槽候选（组件 310）；
  - 角色子职业 bucket 的物品清单（每个元素的子职业物品 hash + 实例 ID + 是否装在身上）；
  - 一个具体模组 hash 落在哪个槽的候选里。
- 产出：写进本文档「实采记录」一节（槽位数量、索引与模组对应关系）。

### P1 神器模组：按 hash 找槽

- `artifact_service.equip_artifact_mod`：删掉 `socket_index=0`，改成
  "在该神器实例的候选里找包含这个模组 hash 的槽 → 用那个索引插入"；
- 找不到就**如实失败**，码要选对（这是审核发现的矛盾点）：**"模组不在候选/未解锁"属于调用方给错了目标
  → 服务里 `raise InvalidArgumentError`（→ `invalid_argument_error`）**；而"上游拒绝这次写入"属于写入失败
  → 保持写入族码 `equip_artifact_mod_failed`。两者消息都要说清是哪种；
- 插入后**回读核对**：重新读 305 确认该槽 `plugHash == 目标 hash` 再报成功——注意**必须绕过 profile 缓存**
  （项目里有"显式新读"的开关，`test_explicit_fresh_read_bypasses_cache` 守的就是这件事），
  否则会拿着旧值报"成功"（假成功比失败更糟）；
- 返回载荷给 `{slot_index, from, to}`（与护甲 `equip_mod` 同一风格），不只给一句 message；
- 测试：单测（用夹具的 socket 结构，覆盖"命中/未命中/多槽候选"三种）+ 真机语料行（装一个真模组 + 回读）。

### P2 换子职业元素：装备另一件子职业物品

- **关键事实（决定实现方式）**：每件子职业物品**各自保留自己的插槽配置**。所以"只换元素、不改槽"的请求
  = **一次 equip 就够**，不需要任何 plug 写入；只有同一请求里还带了插槽变更时才需要后续写槽。
- `subclass_assistant(intent="modify")` 新增一个变更键：
  `changes={"subclass": "烈日"}` → **解析顺序固定**：先查元素别名表 → 再在该角色的子职业物品里按 Manifest
  官方名匹配 → 都不中则报错并**列出该角色可用的子职业（元素 + 官方名）** → 从角色背包找到目标物品**实例**
  → 走既有 equip 流程装上 → **然后**才做同一请求里的其它 plug 变更（顺序有要求：先换物品再改槽，
  旧物品的槽索引在新物品上没意义）；
- **幂等**：当前已经是目标子职业时**不做 equip**（只处理插槽变更），避免白写一次；
- `loadout_subclass_sockets._apply_subclass_config` 的"子职业不一致就失败"**要不要改成"先换再配"是产品决定**
  （见「待决定」第 4 条）：那会改变 `equip_loadout` 的语义——以前"子职业不对就拒绝"，改完就变成"顺手帮你换"；
- 换完回读：`subclass get` 的元素/子职业名必须与请求一致，插槽变更逐项核对；
- 测试：单测（元素解析、找不到该元素物品、不是该角色/不在背包）+ 真机语料行
  （术士切烈日 → 回读 → 切回棱镜 → 回读）。

### P3 收敛与登记

- 参数契约：`tools/_param_contracts.py` 登记新键的归属；`changes` 的合法键加入校验；
- 文档：`skills/destiny2-mcp/references/routing.md`（契约测试会校验）、README 的能力表；
- 语料：`TESTING_CORPUS_FULL.md` 加两行；`run_corpus_all_rows.py` 的 rows 组加断言；
- `COMPATIBILITY.md`：新键是**新能力**（不是别名），登记一句；旧行为（"子职业不一致就失败"）直接删除，
  不留兼容；
- 版本：0.3.0（新增能力 + 行为变更），CHANGELOG 一段。

## 五、验证方式（每阶段都要）

| 阶段 | 验证 |
| --- | --- |
| P0 | 只读，无账号变更；产出实采记录 |
| P1 | 单测 + 真机：装一个模组 → 回读 305 确认 → （可选）换回原模组 |
| P2 | 单测 + 真机：术士切烈日 → 回读 → 切回棱镜 → 回读；每次写入前后各读一次 |
| P3 | `pytest -q` + `verify_mcp.py` + `run_corpus_all_rows.py --group rows` |

**写入纪律**：这两件事都会改账号，但都**可逆**（模组可换回、子职业可切回）。执行前逐次说明"要改什么"，
得到你明确同意再写；每次写完回读核对并报告。

## 六、风险与边界

- **神器是赛季制**：赛季切换后模组集合会变，未解锁的模组本来就插不上——失败信息要区分"未解锁"与"槽不存在"；
- **神器重置槽**（DIM 里的 `RESET_SOCKET`）：重置是**付费/消耗**操作，本次**不实现**，遇到就如实说；
- **子职业物品必须在背包/身上**：不在的话要先取回（`pull_postmaster`/`move`），不能凭空造；
- **顺序**：换子职业物品之后再改槽；反过来会拿旧槽索引改新物品，必然错；
- **上游权限**：神器的付费操作可能需要 `AdvancedWriteActions`（我们目前没开），免费插入不受影响；
- **时机**：在副本/活动中、或角色正装备着该子职业时，上游可能拒绝——失败信息要如实转述上游原文，
  不能包装成"你的账号有问题"；
- **回读纪律**：所有"写完核对"都必须绕过缓存（见 P1），并且 P2 的真机验证要**先存一份当前棱镜配置作为快照**，
  切回后**逐项比对**，确认没把你的配置弄丢。

## 七、输入词表：键名只有一个，值可以很多

**键名定死 `subclass`**（改的是"哪套子职业"，不是抽象的"元素"）；纠结的应该是**值**——
中文玩家对元素的叫法太多，而且**按职业不同**：术士说"火术/电术/冰术/缚丝术/棱镜术/虚空术"，
猎人会说"火猎"、泰坦会说"火泰"。所以：

- **规范键（内部唯一）**：`solar` / `arc` / `void` / `stasis` / `strand` / `prism`；
- **输入别名（可很多）**：中英、官方名、玩家口语、按职业的口语、以及 DIM 的用语（火=Thermal）全收，
  一律映射到规范键。别名表进 `destiny_mcp/vocabulary.py`（和六维/职业同一套规矩），
  配"禁止再抄"与"每个别名必须能解析"的守门测试；
- **子职业物品的官方中文名从 Manifest 读**（每个元素的子职业物品 `displayProperties.name`），
  不写进别名表——名字的权威来源是 Manifest，这条和项目既有规矩一致。

### 7.1 拟定的别名表（待你补/改）

| 规范键 | 官方中文（元素） | 上游/英文 | 术士（X术） | 猎人（X猎） | 泰坦（X泰坦） |
| --- | --- | --- | --- | --- | --- |
| `solar` | 烈日 | solar、Thermal（DIM） | **火术**、火中术、烈日术 | **火猎** | **火泰坦**（另收 火泰） |
| `arc` | 电弧 | arc | **电术** | **电猎** | **电泰坦**（另收 电泰） |
| `void` | 虚空 | void | **虚空术** | **虚空猎** | **虚空泰坦**（另收 虚空泰） |
| `stasis` | 冰影 | stasis | **冰术** | **冰猎** | **冰泰坦**（另收 冰泰） |
| `strand` | 缚丝 | strand | **缚丝术**、编织术（旧写法） | **缚丝猎** | **缚丝泰坦**（另收 缚丝泰） |
| `prism` | 棱镜 | prism、prismatic、Kinetic（DIM） | **棱镜术** | **棱镜猎** | **棱镜泰坦**（另收 棱镜泰） |

加粗的是你确认过的叫法（术士"X术"、猎人"X猎"、泰坦用**全称**"X泰坦"）；括号里的短写"X泰"
是顺手多收的兜底，不是主叫法。猎人/泰坦两列其余写法按同一构词法补，**发现新叫法就往这张表里加**。

### 7.2 匹配规则：只查表，不猜

1. 先归一化输入（去空格、大小写统一、全角转半角）；
2. **查表**（精确匹配，命中即返回规范键）；
3. 查不到再按**职业后缀**拆一次：去掉结尾的 `术士/猎人/泰坦/术/猎/泰`，剩下的部分必须是已知元素词
   （火/烈日/电/电弧/冰/冰影/虚空/缚丝/编织/棱镜），拆不出来就**报错并列出可用叫法**；
4. **不做模糊匹配、不做拼音、不猜**——猜错比报错更坑（这一条与项目"不许静默降级"同一原则）。

### 7.3 两条判定规矩

1. **职业与叫法冲突时报错而不是硬来**：目标角色是猎人、却说"火术"，就回一句
   "「火术」是术士的叫法；你要切的是猎人，猎人对应的是「火猎」。要按哪个来？"
   ——不能悄悄切了另一个职业的子职业；
2. **建筑/流派绰号只当元素线索，不当完整请求**：比如"火中术"（光焰之井那套）含义是"烈日术士"，
   我们**切到烈日**，然后明说"星相/碎片（光焰之井）要另外配，要不要按某套模板来"——
   只做元素部分是诚实的，不假装把整套 build 也换了。

## 八、待决定（需要你拍板）

1. **别名表**：已按你给的构词法定稿（术士 X术、猎人 X猎、泰坦 X泰坦）；以后见到新叫法往表里加即可；
2. ~~是否允许真机写入验证~~ 已定：允许；
3. ~~你遇到时的报错原文~~ 已拿到（见「九」），下面是读出来的发现（哪个 intent、`code`/`message`）——能帮我确认是不是还有第三个原因（比如权限或未解锁）；
4. ~~`equip_loadout` 的语义~~ **已定：顺手换**（用户 2026-09-15 拍板）。所以
   `loadout_subclass_sockets._apply_subclass_config` 的"子职业不一致就失败"**改成"先换再配"**，这条从待决定变成必做；
5. ~~是否允许真机写入验证~~ **已定：允许**（P1/P2 各一次，仍按"先说改什么、写完回读"的纪律执行）。

## 实采记录（P0，2026-09-15，只读）

**神器（组件 305，两个角色各一件实例）**

- 实例：`6917530182721940425`、`6917530182721941207` → 确认**神器是每个角色一份**（读取侧该认 `character`）；
- **槽位数 = 8**；每个槽当前装着同一个 plug `1235188590`（看着是"空/占位"）；
- **槽条目里没有 `socketIndex` 字段** → 插入请求里的槽号只能取**数组位置**（0..7）；
- **用组件 310（reusablePlugs）按槽查候选得到 0** → 神器的可选模组**不是**从 310 来的。
  结合 DIM 的 `socket.plugSet`（定义级插槽数据），P1 应改为：
  **读神器物品的 Manifest 定义 → 每个 socket entry 的 plug set（`reusablePlugSetHash`）→ 找出哪个槽的
  plug set 包含目标模组 hash → 用那个槽的数组位置去插**。
  （当前 8 个槽装的是占位 plug，所以"哪个槽装哪个模组"只能从定义推，不能从现状读。）

**读路径是否走缓存（代码证据）**

`services/player_resolver.py:83 get_profile` 的 docstring 明写 **"Fetch profile from Bungie API (no cache)"** →
子职业/神器的读不走 profile 缓存。所以链条里"切完还是棱镜"最可能是**游戏里没切成功**（或切在别的角色上），
而不是我们读了旧值；但"写完回读走新读"的纪律仍然保留（写入流程另有缓存层，见 `profile_cache`）。

**P0 顺带发现的裸抛（另案）**

`resolve_player("")`（空玩家名）会拿空串去搜玩家 → 上游报错 → aiobungie 在解析错误时抛
**裸 `KeyError: 'membershipType'`**，不是我们的错误信封。工具的默认玩家路径填了名字所以没暴露，
但任何"把空名透传给 resolver"的路径都会裸抛。待定：与本次一起修，还是单独开一条。

**神器定义的槽位规则（P1 的实现依据，实采）**

神器「NPA斥力调节器」（hash `1504442987`）定义里 **8 个 socket entry，4 组 plug set**：

| 槽（数组位置） | plug set | 候选数 | 说明 |
| --- | --- | --- | --- |
| 0、1 | `3122605097` | 8 | 同一阶，两个**可互换**的槽 |
| 2、3、4 | `3122605098` | 15 | 同一阶，三个可互换槽 |
| 5、6 | `3122605099` | 22 | 同一阶，两个可互换槽 |
| 7 | `3730151587` | 2 | 只有「重置神器 / 空神器模组」→ **付费重置槽**，本次不实现 |

- **插入规则**：找出"候选里包含目标模组 hash"的那个槽 → 用它的**数组位置**当 `socketIndex`；
- **同阶多槽**：优先挑当前装着「空神器模组」的槽；全满则如实说"这一阶的槽都占满了，要先空出一个"，
  不擅自顶掉某个已装模组（那会悄悄改掉你别的东西）；
- 组件 305 的槽条目**没有 `socketIndex` 字段**，所以位置只能按数组下标算（见上）。

**各元素子职业物品的官方中文名（Manifest 实测，用于 `subclass` 值解析）**

| 元素 | 猎人 | 术士 | 泰坦 |
| --- | --- | --- | --- |
| solar | 枪手 | **破晓** | 炎阳 |
| arc | 神行 | 唤雷 | 强袭 |
| void | 夜潜 | 虚行 | 哨兵 |
| stasis | 冰魂 | 影宗 | 巨兽 |
| strand | 丝线跑手 | 虫群织工 | 狂战士 |
| prism | 棱镜猎人 | 棱镜术士 | 棱镜泰坦 |

（解析顺序仍是：元素别名表 → 该角色背包里的这些官方名 → 都不中则报错并列出可用项。）

**读路径复核：读的是"身上那件"，正确**

决定性实验（不改账号）：此刻术士身上是**破晓（烈日）**，`subclass get` 也报 **破晓 / hash 3941205951** ✓。
所以 **不是"读错物品"**——更正先前猜测：链条里"用户说切好了但仍是棱镜"，更可能是**当时游戏里没切成功**
（之后切成功了，现在读到的就是破晓）。读取这条不需要改。

**空名裸抛（复核，仍存在）**

`resolve_player("")` 再次复现：拿空串搜玩家 → 上游报错 → aiobungie 抛裸 `KeyError: 'membershipType'`。

## 九、用户调用链读出来的三个新发现（2026-09-15）

原始链条：`subclass get`（棱镜术士）→ `options(element=烈日, component=super)`（光焰之井…）→
`modify changes={"super": "光焰之井"}, confirmed=true` **失败**（"可用超能列表里没有光焰之井"）→
用户说"切好了" → `subclass get` **仍是棱镜、`subclass_hash=3893112950` 没变** → 再查一次仍不变 →
`artifact`（先误传 `character` 被 `ignored_parameter` 拒）→ 拿到 35 个模组 → **没有尝试 `equip_artifact_mod`**。

### 9.1 【最重要】可能存在第三个原因：**读档是旧的**（假阴性）

链条里"用户说切好了、工具连查两次仍是棱镜"有两种解释：① 用户其实没切；② **我们的读走了 profile 缓存**，
所以显示的是切之前的状态。如果是 ②，那用户感知到的"切不了"里有一部分根本不是能力缺失，而是**回读是旧值**——
而且这会直接毁掉写入验证（写完回读还是旧值 → 报假失败或假成功）。

**P0 必须加一项**：不动任何东西，连续两次读 `subclass get`（第二次走**显式新读**），
比对 `subclass_hash`，确认"读是否新鲜"；所有"写完回读"一律绕缓存（与 P1 同一条纪律）。
项目里已有现成机制（`test_explicit_fresh_read_bypasses_cache` 守的就是它），P0 要确认子职业这条路用上了。

### 9.2 `modify` 的失败话术在误导调用方

它说"可用超能列表里没有光焰之井"——**事实对，原因错**：真正的原因是"光焰之井属于烈日，而你现在是棱镜"。
P2 的解析器上线后应改成：

> 光焰之井属于**烈日**，你现在是**棱镜**。要我把子职业切到烈日再装它吗？

（同一句话里给出下一步，而不是让 Agent 自己猜"是不是要手动切"——链条里 Agent 就猜成了"API 不支持"。）

### 9.3 神器的读取与写入不对称，而且读取不带解锁状态

- `artifact` **不读 `character`**（链条里被 `ignored_parameter` 拒），但 `equip_artifact_mod` 是**按角色**
  找神器实例的——神器模组的配置本来就是每个角色各一份。**读写不对称**，P3 改成"读也认 `character`"，
  返回该角色神器的**已装模组 + 各阶模组解锁状态**；
- 链条里 Agent **一次都没试** `equip_artifact_mod`，自己推断"没解锁所以不行"就停了。
  所以读取必须带**解锁标记**，失败信息要列"这一阶已解锁、可选的有哪些"——否则 Agent 只能瞎猜或干脆不动手。

### 9.4 P0 的执行纪律（我自己踩过两次）

**不要手写组件号**：`services/profile_components.py` 里已有现成组合——
`INVENTORY_SOCKETS=[…,305]`、`WEAPON_DETAIL=[…,310]`、`ARTIFACT=[…,300,302]`、`FULL=[…,305,310]`。
我第一次用猜的 205/305/310 被上游拒；第二次把常量当成 int（其实是列表）脚本直接报错。
P0 直接 `from destiny_mcp.services import profile_components` 取现成列表。

## 十、追加要求（2026-09-15，用户提出）：神器不止一个、模组要能换、神器也要能换

### 10.1 不许写死某一个神器的结构

实采已经证明：神器槽位不是固定 8 个——不同赛季的神器**槽数与阶数可能不同**（本次那件是 8 槽 4 组 plug set）。
所以 P1 实现必须**每次都用"角色身上那件神器实例的 itemHash"去读定义**，动态解析：
socket entry 数 → 每组 plug set → 候选模组。**代码里不许出现任何具体神器的 hash 或槽数。**

### 10.2 模组要能换（不是"满了就拒绝"）

修正 P1 里"同阶槽满则拒绝"的写法：游戏里同阶模组本来就是**可自由替换**的。所以：

- 有**空槽**（装着「空神器模组」）→ 直接装，返回 `{slot_index, from: null, to}`；
- 该阶**已满** → 不能默默顶掉别人的模组，但也不能一拒了之。两种做法（都在计划允许范围）：
  ① 调用方显式说明要顶掉哪个（把"要移除的模组名"一起传进来）；
  ② 只给意图（装 Y）→ 返回**确认请求**："烈焰这一阶已满，装「Y」需要顶掉「X」，确认吗？"，
     确认后再写（和项目既有的 `confirmation_required` 一套流程）。
- 每次替换都要**绕缓存回读**核对，并返回 `from → to`。

### 10.3 神器本身也要能换

需求："神器也要能换" = 支持把**另一件神器**装到角色身上（或至少支持指定用哪一件）。
这需要先勘探清楚（P0-b）：

1. 这个账号**所有**神器实例在哪（角色背包 / 账号仓库 / 邮政长）？各自 itemHash 与 Manifest 名称是什么？
2. 神器所在的 bucket（`1506418338`：Seasonal Artifact）**能不能通过 API 装备**别的神器？
   （上游可能拒；拒的话如实说"神器不能远程换"，不能假装成功。）
3. **读到的"当前神器"到底对不对**——实采出现一处不一致：角色身上神器实例的 hash 是 `1504442987`
   （Manifest 名 **NPA斥力调节器**），而 `subclass_assistant(intent="artifact")` 报的 `current_artifact`
   是 **好奇之器**。两者至少有一个是错的，P0-b 要判定：
   - 若 `get_current_artifact()`（现实现按"最高 id"猜）不可靠 → 改成**以角色身上那件实例为准**；
   - 若只是名字查错 → 修名字来源。

### 10.4 P0-b 勘探清单（只读）

- [ ] 列出该账号所有神器实例（三个 bucket + 邮政长），含 itemHash / 名称 / 光等 / 位置
- [ ] 确认"角色身上那件"是否唯一，以及它的 itemHash 对应的 Manifest 名
- [ ] 对比 `artifact` 读取返回的 `current_artifact` 与"身上那件"，判定哪个对
- [ ] 试一次 `EquipItem` 到 Seasonal Artifact bucket 的**可行性**（先不动账号：只查上游文档/SDK 支持，
      必要时在真机验证阶段征得同意后实测一次，可逆）
- [ ] 记录不同神器的槽结构差异（再取一件旧神器定义对比 socket 数与 plug set 组数）
