"""随包分发的本地数据（`data/` 目录）的包标记。

这里以前只放 JSON/YAML（选取率快照、社区名映射、愿单），Python 模块从 2026-09-17 起
也可以放在这里：**只放"事实表"**（实测出来的对照关系，没有逻辑），有逻辑的仍然归
`services/`。第一个这样的模块是 `pvp_counters.py`（计数器 hash → 模式/周期）。

为什么要单独一层包而不是塞进某个服务：这些表要被服务层与工具层**共用**，
又不能反过来依赖它们（分层只允许向下 import）。
"""
