{
  "_note": "护甲面的「允许消失」登记：键是路径（list 索引写成 []），值写明去向。三种键都认：`用例::路径`（只在某个用例消失）、`路径`（所有用例）、`用例::块根`（子树，字段整块改名时用）。没登记的消失会让 scripts/diff_weapon_baseline.py 退出码非 0。P0 建立时为空 —— 护甲重构（ARMOR_FORMAT_PLAN.md）改键名时在这里逐条登记去向。",
  "removed": {}
}
