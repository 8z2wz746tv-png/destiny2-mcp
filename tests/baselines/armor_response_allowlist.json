{
  "_note": "护甲面的「允许消失」登记：键是路径（list 索引写成 []），值写明去向。三种键都认：`用例::路径`（只在某个用例消失）、`路径`（所有用例）、`用例::块根`（子树，字段整块改名时用）。没登记的消失会让 scripts/diff_weapon_baseline.py 退出码非 0。P0 建立时为空 —— 护甲重构（docs/plans/ARMOR_FORMAT_PLAN.md）改键名时在这里逐条登记去向。",
  "removed": {
    "equip_mod_confirm::candidates[].alternatives[].stat_bonus_hashes": "0.1.4：alternatives 的加成改用与 to.stat_bonus 同一套六维可读键（stat_bonus），原始 hash（含非六维的费用属性 3578062600）不再外露 —— 那个字段会让调用方把「费用 1」读成「有加成」。见 tests/test_equip_mod.py 的同名断言。",
    "ladder_no_solution::data.ladder": "0.1.6：这条用例（猎人 武器150/职业100/超能80/近战70/手雷70 + 金装「快速装弹松身裤」）以前实测 0 候选，调谐补齐（P7）之后**有解了** —— 5 套候选、每套只需改 1 件调谐就达标，所以整块 ladder 不再出现（ladder 只在 0 解时给）。「真的配不出来」那条路改由新用例 `ladder_infeasible_verdict` 覆盖（`verdict.satisfiable=false` + 逐项 ceiling 说明）。",
    "ladder_no_solution::next_actions": "同上：有解就没有『降级建议』那一组 next_actions。"
  }
}
